
## 🧱 Instruction for creating component

- ✅ Use path aliases (`@/assets/...`, `@/components/...`)
- ✅ Colors and typography must map to `index.css` tokens
- ✅ Use `/assets/placeholder.svg` for all icon placeholders
- ✅ `onClick?: () => void` is for non-returning handlers
- ✅ Text styles come from `index.css` tokens (not raw Tailwind)
- ✅ Spacing values are multiples of 4px (except 2px)
- ✅ Use `<a>` or `<Link>` for navigation — never `<div>` or `<button>`
- ✅ Avoid arbitrary pixel values or hex colors
- ✅ Use `.focus-ring` for keyboard focus visibility
- ✅ Use responsive Tailwind utilities only for layout
- ✅ All components must be keyboard-accessible and follow ARIA patterns
- ✅ Follow 1:1 mapping between Figma tokens and Tailwind config
